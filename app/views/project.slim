div.gallery {
  display: flex
}

  .row
    .img_wrap
    
      img src="https://im.marieclaire.com.tw/m800c533h100b0/assets/mc/201907/5D19A650F04EF1561962064.png"  alt="一張圖片"

  body
    h1 #{shelter_name} Search result: The total #{animal_kind} numbers is 

    div.gallery
      - animal_obj_list.each do |obj| 
        p style="display:inline-block;"
          img src = "#{obj.album_file}" width = 300 height = 300
          br
          | Id : "#{obj.animal_id.to_s}"
          br
          | Sex : "#{obj.animal_sex.to_s}"
          br 
          | Sterilization : "#{obj.animal_sterilization}"
          br 
          | Bacterin : "#{obj.animal_bacterin}"
          br 
          | BodyType : "#{obj.animal_bodytype}"
          br 
          | AnimalPlace : "#{obj.animal_place}"
          br 
          | AnimalDate : "#{obj.animal_opendate}"
      



